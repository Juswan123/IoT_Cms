# Server Configuration
PORT=3000
NODE_ENV=production

# Database Configuration (RDS or Local)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=iot_cms_db

# JWT Authentication
JWT_SECRET=your_super_secret_key_change_this
JWT_EXPIRES_IN=1d

# AWS Configuration (If using S3 for images/logs)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-southeast-1
AWS_BUCKET_NAME=

# IoT / MQTT Configuration (If applicable)
MQTT_BROKER_URL=mqtt://broker.hivemq.com